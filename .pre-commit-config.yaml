repos:
  - repo: local
    hooks:     
      - id: format-validate
        name: Format Validate
        entry: python scripts/format_validate.py || exit 1
        language: system
        always_run: true
        pass_filenames: false