# Proposed Attribute Value Synonyms

The current finding model schema does not support synonyms on choice attribute values. When the schema is upgraded to support value-level synonyms, the entries below should be incorporated into the relevant finding model definitions.

Add entries here when you encounter values that refer to the same concept but use different wording during merge or review operations.

## Format

```
finding: <finding name>
attribute: <attribute name>
canonical value: <chosen value name>
proposed synonym: <alternative wording>
source: <where this was encountered>
```

## Entries

(none yet — add entries as they are encountered during merge and review operations)
